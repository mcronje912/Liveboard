{
  "repository": {},
  "description": " ",
  "license": "MIT",
  "scripts": {
    "deploy": "NODE_ENV=production tailwind --input=css/app.css --output=../priv/static/assets/app.css --minify && NODE_ENV=production esbuild js/app.js --bundle --minify --target=es2017 --outdir=../priv/static/assets"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "autoprefixer": "^10.4.16",
    "esbuild": "0.17.11",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.4.0"
  }
}
